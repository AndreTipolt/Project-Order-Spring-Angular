import { StatusPaymentPipe } from './status-payment.pipe';

describe('StatusPaymentPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusPaymentPipe();
    expect(pipe).toBeTruthy();
  });
});
